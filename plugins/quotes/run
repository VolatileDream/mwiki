#!/usr/bin/env bash

run(){
	local entry="$1" ; shift
	local file="$1" ; shift
	cat "$ile"		|
	grep -E '^!quotes '	|
	# remove the quote specifier
	sed 's_^!quotes \+__'	|
	# add a starting space for subsequent quote lines
	sed 's_\\n_<br>\&nbsp;_g'	|
	awk '{ print "<p>" $0 "</p>" }'
}

run "$@"
